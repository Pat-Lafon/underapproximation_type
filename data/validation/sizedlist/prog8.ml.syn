let rec sized_list_gen = fun s ->
  let (x!2) = sizecheck s in
  match x!2 with
  | True -> [] 
  | False ->
      let (_x61) = bool_gen () in
      (match _x61 with
       | True ->
           let (_x5) = s in
           let (_x8) = subs _x5 in
           let (_x25!3) = sized_list_gen _x8 in
           let (_x4) = int_gen () in Cons _x4 _x25!3
       | False -> Nil )