let rec sized_list_gen = fun s ->
  let (x_2) = sizecheck s in
  match x_2 with
  | True -> [] 
  | False ->
      let (_x62) = bool_gen () in
      (match _x62 with
       | True ->
           let (_x5) = s in
           let (_x8) = subs _x5 in
           let (_485) = sized_list_gen _x8 in
           let (_x4) = int_gen () in Cons _x4 _485
       | False -> Nil )