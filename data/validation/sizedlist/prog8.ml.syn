let rec sized_list_gen = fun s ->
  let (x_2) = sizecheck s in
  match x_2 with
  | True -> []
  | False ->
      let (_x56) = bool_gen () in
      (match _x56 with
       | True ->
           let (_x2) = s in
           let (_x5) = subs _x2 in
           let (_453) = sized_list_gen _x5 in
           let (_x1) = int_gen () in Cons (_x1, _453)
       | False -> Nil)