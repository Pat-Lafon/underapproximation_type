let rec sized_list_gen = fun s ->
  let (x_2) = sizecheck s in
  match x_2 with
  | True -> Nil
  | False ->
      let (_x22) = bool_gen () in
      (match _x22 with
       | True ->
           let (_x2) = s in
           let (_x7) = subs _x2 in
           let (_13) = sized_list_gen _x7 in
           let (_3) = int_gen () in Cons (_3, _13)
       | False -> Nil)