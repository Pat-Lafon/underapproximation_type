let rec sized_list_gen = fun s ->
  let (x_3) = sizecheck s in
  match x_3 with
  | True -> [] 
  | False ->
      let (_x6) = bool_gen () in
      (match _x6 with
       | True -> [] 
       | False ->
           let (_x5) = s in
           let (_x9) = subs _x5 in
           let (_494) = sized_list_gen _x9 in
           let (_x4) = int_gen () in Cons _x4 _494)