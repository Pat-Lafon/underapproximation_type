let rec sized_list_gen = fun s ->
  let (x!3) = sizecheck s in
  match x!3 with
  | True -> [] 
  | False ->
      let (x!4) = bool_gen () in
      (match x!4 with
       | True -> [] 
       | False ->
           let (_x8) = s in
           let (_x9) = subs _x8 in
           let (_x26!1) = sized_list_gen _x9 in
           let (_x7) = int_gen () in Cons _x7 _x26!1)