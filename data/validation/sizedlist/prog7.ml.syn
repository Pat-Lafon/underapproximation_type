let rec sized_list_gen = fun s ->
  let (x_3) = sizecheck s in
  match x_3 with
  | True -> Nil
  | False ->
      let (_x23) = bool_gen () in
      (match _x23 with
       | True ->
           let (_x2) = s in
           let (_x8) = subs _x2 in
           let (_22) = sized_list_gen _x8 in
           let (_11) = int_gen () in Cons (_11, _22)
       | False -> Nil)