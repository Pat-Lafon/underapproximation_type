let rec duplicate_list_gen = fun s ->
  fun x ->
    let (x!7) = sizecheck s in
    match x!7 with
    | True -> Nil
    | False ->
        let (x!8) = subs s in
        let (x!9) = duplicate_list_gen x!8 in
        let (x!10) = x!9 x in
        x :: x!10