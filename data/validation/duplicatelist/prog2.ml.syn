let rec duplicate_list_gen = fun s ->
  fun x ->
    let (xccc2) = sizecheck s in
    match xccc2 with
    | true -> []
    | false ->
        let (idx1ccc0) = s in
        let (idx6ccc0) = subs idx1ccc0 in
        let (idx7) = duplicate_list_gen idx6ccc0 in
        let (idx2ccc1) = x in
        let (idx8ccc1) = idx7 idx2ccc1 in
        let (idx2ccc0) = x in idx2ccc0 :: idx8ccc1