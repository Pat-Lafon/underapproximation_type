let rec sized_list_gen = fun s ->
  let (xccc3) = sizecheck s in
  match xccc3 with
  | true -> []
  | false ->
      let (idx3) = bool_gen () in
      (match idx3 with | true -> [] | false -> hidden_list_gen ())