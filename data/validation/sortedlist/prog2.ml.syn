let rec sorted_list_gen = fun s ->
  fun x ->
    let (x_3) = sizecheck s in
    match x_3 with
    | True -> []
    | False ->
        let (y) = int_gen () in
        let (x_4) = x <= y in
        (match x_4 with
         | True ->
             let (_x1) = s in
             let (_x11) = subs _x1 in
             let (_x114) = sorted_list_gen _x11 in
             let (_122) = _x114 y in Cons (y, _122)
         | False -> Err)