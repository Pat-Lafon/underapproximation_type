let rec sorted_list_gen = fun s ->
  fun x ->
    let (x_6) = sizecheck s in
    match x_6 with
    | True -> Nil 
    | False ->
        let (y) = int_gen () in
        let (x_7) = x <= y in
        (match x_7 with
         | True ->
             let (size2) = subs s in
             let (x_8) = sorted_list_gen size2 in
             let (l) = x_8 y in let (l2) = y :: l in l2
         | False -> Err)