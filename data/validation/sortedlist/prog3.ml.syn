let rec sorted_list_gen = fun s ->
  fun x ->
    let (x_3) = sizecheck s in
    match x_3 with
    | True -> Nil
    | False ->
        let (y) = int_gen () in
        let (x_4) = x <= y in
        (match x_4 with
         | True ->
             let (_x1) = s in
             let (_x7) = subs _x1 in
             let (_x62) = sorted_list_gen _x7 in
             let (_x2) = x in let (_1095) = _x62 _x2 in Cons (y, _1095)
         | False -> Err)