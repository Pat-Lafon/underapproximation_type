let rec sorted_list_gen = fun s ->
  fun x ->
    let (x!3) = sizecheck s in
    match x!3 with
    | True -> Nil 
    | False ->
        let (y) = int_gen () in
        let (x!4) = x <= y in
        (match x!4 with
         | True ->
             let (_x5) = s in
             let (_x11) = subs _x5 in
             let (_x143) = sorted_list_gen _x11 in
             let (_x6) = x in let (_x144!5) = _x143 _x6 in Cons y _x144!5
         | False -> Err)