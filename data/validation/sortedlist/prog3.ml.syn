let rec sorted_list_gen = fun s ->
  fun x ->
    let (x_3) = sizecheck s in
    match x_3 with
    | True -> Nil
    | False ->
        let (y) = int_gen () in
        let (x_4) = x <= y in
        (match x_4 with
         | True ->
             let (_x1) = s in
             let (_x8) = subs _x1 in
             let (_x12) = sorted_list_gen _x8 in
             let (_86) = _x12 y in Cons (y, _86)
         | False -> Err)