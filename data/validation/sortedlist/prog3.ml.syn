let rec sorted_list_gen = fun s ->
  fun x ->
    let (x_3) = sizecheck s in
    match x_3 with
    | True -> Nil
    | False ->
        let (y) = int_gen () in
        let (x_4) = x <= y in
        (match x_4 with
         | True ->
             let (_x5) = s in
             let (_x11) = subs _x5 in
             let (_x143) = sorted_list_gen _x11 in
             let (_x6) = x in let (_3161) = _x143 _x6 in Cons (y, _3161)
         | False -> Err)