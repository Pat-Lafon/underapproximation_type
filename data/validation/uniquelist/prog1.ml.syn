let rec unique_list_gen = fun s ->
  let (x!7) = sizecheck s in
  match x!7 with
  | True -> Nil 
  | False ->
      let (x!8) = subs s in
      let (l) = unique_list_gen x!8 in
      let (x) = int_gen () in
      let (x!9) = list_mem l in
      let (x!10) = x!9 x in (match x!10 with | True -> Err | False -> x :: l)