let rec sorted_list_gen = fun s ->
  fun x ->
    let (xccc3) = sizecheck s in
    match xccc3 with
    | true -> []
    | false ->
        let (y) = int_gen () in
        let (xccc4) = x <= y in
        (match xccc4 with
         | true ->
             let (idx1ccc5) = hidden_list_gen () in
             let (idx7) = y in idx7 :: idx1ccc5
         | false -> hidden_list_gen ())